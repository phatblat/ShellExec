{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build",
            "type": "shell",
            "command": "./gradlew --offline --console plain build -x test"
        },
        {
            "taskName": "run example",
            "type": "shell",
            "command": "./gradlew --offline --console plain --project-dir example helloWorld",
            "problemMatcher": []
        }
    ]
}