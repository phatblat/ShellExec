/*
 * settings.gradle
 * SimpleExecExample
 */

rootProject.name = 'SimpleExecExample'

includeBuild('../') {
    dependencySubstitution {
        substitute module('at.phatbl:simple-exec') with project(':')
    }
}
